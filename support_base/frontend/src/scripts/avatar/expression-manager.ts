/**
 * ExpressionManager — vrm-expression-manager.ts そのままコピー
 *
 * services/frontend-patches/vrm-expression-manager.ts (198行) をそのまま使用。
 * A2E サービスから受け取った 52次元 ARKit ブレンドシェイプを
 * GVRM のボーンシステムにマッピングする。
 *
 * ★ このファイルは frontend-patches/vrm-expression-manager.ts のコピー。
 *   変更せずそのまま使用すること。
 */

export { ExpressionManager, type ExpressionData } from '../../../services/frontend-patches/vrm-expression-manager';

// ※ ビルド環境の都合でパスが通らない場合は、
//    vrm-expression-manager.ts をこのディレクトリに直接コピーする。
